{
  "name": "pin-tauri",
  "private": true,
  "version": "0.1.0",
  "description": "Tauri 2 port of the pin-to-do desktop card layer — size spike",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
